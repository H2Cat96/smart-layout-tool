# 智能排版工具 - 使用说明

## 你需要准备什么

1. 电脑上安装好 **Claude Code**（或 Codex）
2. 解压「智能排版工具.zip」到任意位置

## 使用步骤

### 第一步：打开项目

用 Claude Code 打开解压后的文件夹。

### 第二步：输入命令

在对话框输入：

```
/teaching-layout
```

### 第三步：跟着引导走

AI 会依次问你：

1. **选择输出格式** — PDF 还是 IDML
2. **提供 Word 文档** — 把 .docx 文件路径粘贴过来（或拖进终端）
3. **确认输出位置** — 默认在文档同目录下生成

然后等几秒钟，生成完毕后会告诉你输出文件在哪里。

## 常用场景

| 你想做什么 | 怎么说 |
|-----------|--------|
| 单个文档生成 PDF | 直接把 docx 路径发给它 |
| 多个文档合并成一本 | 把文件夹路径发给它，说「合并排版」 |
| 生成 IDML 给设计师 | 说「生成 IDML」 |

## 注意事项

- Word 文档格式请参考「文档撰写规范.md」
- 首次使用会自动安装依赖，需要联网
- 生成的 PDF 是印刷级排版，可直接送印
- IDML 用 InDesign 打开后需重新链接图片（Links 文件夹已生成好）

## 遇到问题？

直接用自然语言描述你的问题，AI 会协助排查。常见情况：

- 「装不上依赖」→ 检查 Python 是否已安装（需要 3.10+）
- 「字体报错」→ 字体已内置在包里，正常不会出现
- 「排版效果不对」→ 检查 Word 文档是否符合撰写规范
