# Word 文档撰写规范

本规范定义了一套通用的内容标记约定。老师按此规范编写 Word 文档，排版工具即可自动识别内容层级和类型。

不同模板使用其中的不同子集——文档中没有用到的类型直接不写即可。

---

## 层级标记体系

文档内容通过固定格式的"开头标记"来建立层级。每种标记独占一段。

| 层级 | 标记方式 | 示例 |
|------|---------|------|
| **一级标题** | `第X讲` / `第X课` / `第X章`（中文数字） | 第三讲 |
| **二级标题** | `【标题文字】`（中文方括号包裹） | 【阅读理解】 |
| **三级标题** | `中文数字、标题`（中文数字 + 顿号） | 一、现代文阅读 |

### 规则

- 一级标题中文数字后**不加标点、不加其他文字**
- 二级标题的 `【答案】`、`【解析】` 有特殊含义（见"答案区"），编写栏目标题时避开
- 三级标题用**顿号**（`、`），不要用句点

---

## 题目与选项

| 类型 | 格式 | 示例 |
|------|------|------|
| 题目 | `阿拉伯数字` + `.` 或 `．` 或 `、` | 1. 题目内容 |
| 选项 | `大写字母` + `.` 或 `．` | A. 选项内容 |
| 子题 | `（阿拉伯数字）` | （1）子题内容 |

### 规则

- 题号用阿拉伯数字（1、2、3…），**不用**中文数字
- 选项字母必须大写（A-E）
- 每道题、每个选项**独占一段**
- 子题用**全角圆括号**：`（1）`，不要写 `(1)`

---

## 正文内容

普通段落直接书写即可。以下是工具支持自动识别的特殊内容：

### 出处 / 来源

独占一段，全角圆括号包裹整段文字：

```
（选自《朱自清散文集》）
（2023年北京中考真题）
```

### 答题线

在 Word 中用连续下划线字符画一条横线即可，排版时自动生成占满版心的答题线。

### 图片

直接插入 Word，自动排版为上下绕排。

### 表格

直接在 Word 中插入表格即可。

---

## 内联格式

在 Word 中对文字施加以下格式，排版工具会自动识别并应用对应效果：

| 格式 | Word 中的操作 | 排版效果 |
|------|-------------|---------|
| 加粗 | 选中文字 → 加粗（B） | 笔画加粗 |
| 着重号 | 选中文字 → 字体 → 着重号 | 文字下方加点 |
| 拼音 | 在字后加全角括号标注拼音：`鍪（móu）` | 上方拼音标注 |
| 上标 | 选中文字 → 上标 | 缩小并上移 |
| 下划线 | 选中文字 → 下划线 | 带下划线 |

拼音规则：拼音音节数决定标注的汉字数。`鍪（móu）` → 标注1个字；`兜鍪（dōu móu）` → 标注2个字。

---

## 答案区（可选）

如果文档需要答案部分，在所有练习内容之后，加一个**答案主标题**。该段之后的所有内容都归入答案区。

没有答案的文档不写这部分，所有内容按正文/练习排版。

### 答案主标题

```
参考答案与解析
参考答案及解析
答案与解析
```

### 答案标签（分隔答案与解析）

```
【答案】
【解析】
答案：
解析：
```

### 答案内容

题号格式和练习部分相同（`1. 内容`）。

---

## 通用原则

1. **一段一意**：每个标题、每道题、每个选项都独占一段（回车分隔）
2. **格式即结构**：工具通过段落开头的固定格式判断内容类型，不依赖 Word 样式名
3. **全角标点**：涉及结构标记的符号（方括号、圆括号）统一用全角中文符号
4. **用到什么写什么**：文档中没有的层级/类型不需要写，工具不会因为缺少某一层级而报错

---

## 常见错误

| 错误写法 | 正确写法 | 原因 |
|----------|----------|------|
| `第1讲` | `第一讲` | 一级标题用中文数字 |
| `一. 标题` | `一、标题` | 三级标题用顿号 |
| `[栏目名]` | `【栏目名】` | 用中文方括号 |
| `a. 选项` | `A. 选项` | 选项字母大写 |
| `(出处)` | `（出处）` | 用全角圆括号 |
| `(1).内容` | `（1）内容` | 子题括号后不加句点 |

---

## 速查表

```
第X讲 / 第X课          → 一级标题
【XX】                  → 二级标题
一、XX                  → 三级标题
1. / 2. / 3.           → 题目
A. / B. / C. / D.      → 选项
（1）/（2）              → 子题
（全角括号包裹整段）      → 出处
连续下划线              → 答题线
参考答案与解析          → 答案区开始
【答案】/【解析】        → 答案标签
Word加粗               → 加粗
Word着重号             → 加点
字（pīnyīn）           → 拼音标注
```
